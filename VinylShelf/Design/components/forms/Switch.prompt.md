**Switch** — binary toggle for settings ("Public shelf", "Notify on wishlist match"). Yellow + glow when on; knob springs across.

```jsx
<Switch checked={isPublic} onChange={setIsPublic} label="Public shelf" />
```
