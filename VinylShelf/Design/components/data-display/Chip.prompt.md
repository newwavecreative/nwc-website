**Chip** — pill for genres, formats, and filter rows. Toggle `selected` for filter state.

```jsx
<Chip selected={g==='Jazz'} onClick={() => setG('Jazz')}>Jazz</Chip>
<Chip iconLeft={<i data-lucide="disc" />}>2×LP</Chip>
```
