import SwiftUI

extension Color {
    /// Primary accent — warm amber/gold #C9943A. Defined once; use everywhere.
    static let vinylAccent = Color(red: 0xC9 / 255, green: 0x94 / 255, blue: 0x3A / 255)
}
