import Foundation

/// TEMPLATE — copy this file to `DiscogsConfig.swift` (same folder) and paste
/// in your personal Discogs API token. `DiscogsConfig.swift` is gitignored so
/// the token never lands in version control.
///
/// Generate a token at https://www.discogs.com/settings/developers
///
/// Never log or print the token.
enum DiscogsConfig {
    static let apiToken = "YOUR_DISCOGS_TOKEN_HERE"
}
